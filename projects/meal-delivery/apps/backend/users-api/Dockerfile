# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build meal-delivery-users-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t meal-delivery-users-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system meal-delivery-users-api && \
          adduser --system -G meal-delivery-users-api meal-delivery-users-api

COPY dist/projects/meal-delivery/apps/users-api meal-delivery-users-api/
RUN chown -R meal-delivery-users-api:meal-delivery-users-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix meal-delivery-users-api --omit=dev -f install

EXPOSE 3000

CMD [ "node", "meal-delivery-users-api" ]
